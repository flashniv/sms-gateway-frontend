import {Box} from "@mui/material";

export default function Dashboard() {
    return(
        <Box
            sx={{
                backgroundColor:"green"
            }}
        >
            text
        </Box>
    );
}